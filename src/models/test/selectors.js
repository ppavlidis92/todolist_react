export const textValue = (state) => state.textFieldValue;
export const password = (state)=> state.passwordValue;
export const isLoggedIn = (state)=> state.isLoggedIn;
export const count = (state)=> state.count;